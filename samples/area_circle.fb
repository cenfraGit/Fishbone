println("Program: area of a circle\n");

func get_circle_area(radius)
{
    return PI * pow(radius, 2);
}

print("Enter the circle's radius: ");
let radius = double(input());
let area = get_circle_area(radius);

print("The area of the circle is: ");
println(round(area, 3));