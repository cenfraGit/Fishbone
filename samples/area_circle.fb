print("Program: area of a circle\n");

func get_circle_area(radius)
{
    return PI * pow(radius, 2);
}

println("Enter the circle's radius: ");
let radius = double(input());
let area = get_circle_area(radius);

println("The area of the circle is: ");
print(round(area, 3));